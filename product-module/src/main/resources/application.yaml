server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Superlinear?serverTimezone=Asia/Taipei&characterEncoding=utf-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: product-module
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            spl: trace