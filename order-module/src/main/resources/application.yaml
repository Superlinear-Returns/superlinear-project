server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ZMS_USR?useSSL=false&serverTimezone=Asia/Taipei&allowPublicKeyRetrieval=true
    username: root
    password: Az456789
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: order-module
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: TRACE